import StandardLayout from "./(withHeaderFooter)/layout";
import Hero from "@/components/Hero/Hero";

const HomePage = () => {
  return (
    <StandardLayout>
      <Hero />
    </StandardLayout>
  );
};

export default HomePage;
